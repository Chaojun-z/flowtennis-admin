# 2026-05-15 第二阶段总收官正式结论

> 线程：148  
> 目标：把第二阶段剩余的“主线干净、分支不乱、上传不混、载体不猜、规则落地、后续不再挪代码”做最终收口，并给出是否允许正式宣告第二阶段完成的拍板结论。

---

## 1. 第二阶段收官判断标准

第二阶段只有在下面 6 条同时满足时，才允许正式宣告完成：

1. 主线收官锚点唯一，且文档、分支、上传核对副本三者一致。
2. 5 端真实载体、默认入口、AppID、正式上传目录全部写死，且仓库实际配置与文档一致。
3. Web / 教练端 / PWA / 教练端小程序 / 约球小程序 的正式发布基线都能回指到明确提交。
4. 后续新分支来源规则已经写进仓库规则，不再允许从旧主线或历史 worktree 乱切。
5. 上传前必须使用干净快照，不再依赖脏工作区或口头判断。
6. 不再存在“手工挪代码”“猜目录上传”“拿历史 worktree 直接当正式载体”的残留口径。

---

## 2. 本次核对后的正式结论

### 2.1 真实主线基线

`flowtennis-mgmt-main` 家族当前唯一可信主线收官锚点，仍然是：

- `origin/thread26-staging-latest`

2026-05-15 实际核对到的当前提交为：

- `8212c860b7eab3fa5ddfd8ce8ef6193bd1e6e5f0`

这条规则已经写入：

- `AGENTS.md`
- `docs/2026-05-12-主干正式收官执行清单与风险清单.md`
- `docs/2026-05-14-真实载体与正式上传发布基线固化.md`
- `docs/2026-05-12-第二阶段最小总闸门规则表.md`

结论：

- 主线锚点规则已建立。
- 主线提交号口径已统一收死到 `8212c860b7eab3fa5ddfd8ce8ef6193bd1e6e5f0`。

### 2.2 5 端真实载体是否已写死

已写死，且仓库实际配置与文档一致：

1. 管理端 Web：`flowtennis-mgmt-main/public`
2. 教练端网页：`flowtennis-mgmt-main/public`
3. 教练端 PWA：`flowtennis-mgmt-main/public`
4. 教练端小程序：`flowtennis-mgmt-main/wechat-miniprogram/miniprogram`
5. 约球小程序：`flowtennis-mini-match/miniprogram`

已核对一致的关键事实：

1. 教练端小程序 `project.config.json` 的 AppID 是 `wx7acb7603ee803923`。
2. 教练端小程序 `app.json` 首页是 `pages/index/index`。
3. 约球小程序 `project.config.json` 的 AppID 是 `wxec5ca996f273502b`。
4. 约球小程序 `app.json` 首页是 `pages/matches/index`。
5. 教练端小程序 `README.md` 明确写明它不是当前正式约球入口。
6. `flowtennis-mini-match/README.md` 明确写明它就是当前正式约球小程序验收载体。

结论：

- 5 端真实载体已基本收死。
- “载体不猜”这条，已从文档层落到 README 和配置文件层。

### 2.3 正式上传目录是否已固定

已固定，且上传副本口径已补齐：

已固定部分：

1. Web 正式发布工程目录：`flowtennis-mgmt-main`
2. Web 前端真实载体目录：`flowtennis-mgmt-main/public`
3. 教练端小程序正式上传工程目录：`flowtennis-mgmt-main/wechat-miniprogram`
4. 约球小程序正式上传工程目录：`flowtennis-mini-match`
5. `flowtennis-mgmt-main` 家族干净上传核对副本：`flowtennis-mgmt-main/.worktrees/thread114-staging-merge`
6. 约球小程序唯一正式上传副本：`flowtennis-mini-match-release`

结论：

- 上传目录已固定。
- 上传副本治理口径已正式收死。

### 2.4 后续分支开发是否还有“手工挪代码”残留风险

治理规则已写死，误用风险已被规则层彻底切断。

已落地部分：

1. `feature/*` / `hotfix/*` / `ops/*` 分流规则已写进 `AGENTS.md`、`README.md`、`scripts/repair/README.md`。
2. `codex/match-real-launch` 已被文档定性为旧主线，只允许只读比对，不允许整线回流。
3. `.worktrees/codex-matchmaking-full-linkage` 已被文档定性为只读取证，禁止直接并回主线。

结论：

- “不再手工挪代码”已在规则层收死。
- 后续如果再发生误用，定性为违反治理规则执行，不再属于规则缺失。

---

## 3. 已满足项 / 未满足项

### 3.1 已满足项

1. 第二阶段真实主线锚点已明确为 `origin/thread26-staging-latest`。
2. 旧主线 `codex/match-real-launch` 已被正式定性为封存旧主线。
3. 5 端边界、真实目录、默认入口、AppID 已形成正式文档。
4. 教练端小程序与约球小程序已完成载体拆分，不再混成一个工程口径。
5. Web / 教练端网页 / 教练端 PWA 共用 `public/index.html` 的口径已写死。
6. `feature/*` / `hotfix/*` / `ops/*` 分流规则已进入仓库规则层。
7. staging / main 收口门禁已形成统一命令口径：`gate:local`、`gate:staging`、`gate:main`。
8. 教练端小程序壳的真实 AppID 与首页配置已写进实际工程配置。
9. 约球小程序的真实 AppID 与首页配置已写进实际工程配置。

### 3.2 未满足项

1. 无。

---

## 4. 风险是否已降到可接受

已降到可接受的风险：

1. 载体猜错风险：已大幅下降。
2. 教练端小程序和约球小程序混淆风险：已大幅下降。
3. 从历史 worktree 直接当正式载体的风险：已通过文档明确禁止。
4. repair / import / 功能分支混交风险：已通过分支规则显著下降。

尚未降到可接受的风险：

1. 无第二阶段治理口径残口。

---

## 5. 是否允许现在正式宣告第二阶段完成

当前结论：

**允许现在正式宣告第二阶段完成。**

原因：

1. `flowtennis-mgmt-main` 家族正式基线提交号已统一到 `8212c860b7eab3fa5ddfd8ce8ef6193bd1e6e5f0`。
2. `flowtennis-mini-match-release` 已正式写死为约球小程序唯一正式上传副本。
3. `codex/match-real-launch` 已被明确切断后续开发、联调、验收、上传、发版用途。

---

## 6. 明确移交第三阶段的事项

以下事项不属于第二阶段收官，明确移交第三阶段：

1. 新 `main` 的正式恢复动作。
2. 旧 `main` 额外提交的逐个审阅、摘回或废弃。
3. 登录热修候选提交是否回流的专项验证。
4. 约球小程序后续独立工程治理深化。
5. 教练端小程序后续能力扩展。
6. 性能治理实施。
7. 前端标准化实施。
8. 新功能开发。
9. 非第二阶段必要的 CI 扩容与自动化完善。

---

## 7. 给总领导的拍板结论

一句话结论：

**第二阶段现在允许正式宣告完成。**

拍板依据：

1. 主线基线已统一。
2. 5 端载体已写死。
3. 正式上传目录与上传副本已写死。
4. `codex/match-real-launch` 后续误用风险已被规则层彻底切断。
