# 2026-07-14 线索状态与转化链路只读体检表

本报告只读扫描生产实例 `flowtennis-ue`，没有写入生产数据。

## 1. 扫描范围

| 项目 | 数量 |
| --- | ---: |
| 线索 `ft_leads` | 516 |
| 跟进记录 `ft_lead_followups` | 630 |
| 生命周期客户 | 1247 |
| 线索池读模型行 | 958 |

## 2. 代码口径问题

| 问题 | 修复前表现 | 本次处理 |
| --- | --- | --- |
| `dealType=未转化` 被当成已成交 | 线索主状态是跟进中，但生命周期显示已成交 | 已修复：`未转化/未成交` 不再进入成交类型，也不再反推已成交 |
| 成交类型反向篡改线索阶段 | 前端看到 `dealType=课程` 就显示“已成交 · 课程” | 已修复：前端只按统一 `leadStage` 显示阶段，成交类型只做补充展示 |
| 旧体验事实覆盖当前线索阶段 | 当前已流失/跟进中，但生命周期按旧体验时间显示已约体验/已体验待成交 | 已修复：真实成交事实最高优先级；无成交事实时，优先使用线索主状态 |
| 创建学员后购买弹窗找不到学员 | 接口已创建学员，但本地学员列表还没刷新，弹窗提示“学员不存在” | 已修复：接口返回学员后立即写入本地列表，再打开购买弹窗 |

## 3. 存量数据问题

| 类型 | 数量 | 明细 | 处理建议 |
| --- | ---: | --- | --- |
| 已成交课程但没有学员 `studentId` | 3 | 孙姐、Deadia、史多灏 | 不自动批量创建；后续由运营确认后，再创建/关联学员并购买课包 |

## 4. 不做的事

1. 本次不批量改生产数据。
2. 本次不批量创建学员。
3. 本次不批量补课包。
4. 存量 3 条需要用户确认后，单独走生产修数流程。
