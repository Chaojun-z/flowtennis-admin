# 2026-06-27 全仓库口径审计报告

> 本报告只做现状审计，不修业务代码。
> 审计目录：`/Users/shaobaolu/Desktop/FlowTennis/flowtennis-mgmt-main`。
> 审计方式：静态代码扫描 + 关键文件人工核对；未启动 dev server，未打开浏览器，未连接生产数据，未做视觉验收。
> 审计结论：现在不能说“全局统一”，只能说“已有统一文档和部分统一层，但代码仍存在口径不一致、页面本地重算、旧字段扩散、下拉未统一、测试保护旧口径”等问题。

## 1. 覆盖范围

本次按“一个都不能遗漏”的口径覆盖：

1. 管理员侧栏 21 个可见菜单入口。
2. `public/index.html` 里存在但不在侧栏直接展示的可达 / 隐藏页面。
3. 教练端 4 个入口。
4. 后端页面数据接口、读模型、经营指标模型、导入链路。
5. 现有测试。
6. 脚本目录和微信小程序目录的同类风险静态搜索。

## 2. 管理员侧栏 21 个入口审计

| # | 侧栏入口 | 主要代码位置 | 现状 | 发现的问题 |
|---|---|---|---|---|
| 1 | 线索池 | `public/assets/scripts/pages/leads.js`、`server/leads-routes.js`、`server/read-models/platform-metrics.js` | 未统一完成 | 顶部卡片仍在 `leads.js` 用 `leadStatsData()` 本地算预约、已体验、成交、转化率；页面仍展示“意向等级”；导入预览仍展示“意向等级”。 |
| 2 | 普通学员 | `public/assets/scripts/pages/students.js`、`server/read-models/customer-lifecycle.js` | 部分接入生命周期，但未强约束 | 当前视图仍有前端统计，且存在从当前列表倒推体验链风险；若生命周期数据为空，页面仍可能回退到局部数据。 |
| 3 | 正式学员 | `public/assets/scripts/pages/students.js` | 部分统一 | 顶部统计仍由 `studentPageStats()` 汇总购买、权益、财务字段；不是纯展示统一指标 read model。 |
| 4 | 会员管理 | `public/assets/scripts/pages/courts.js`、`server/page-data/core-pages.js` | 部分统一 | 会员状态、等级、余额、折扣等字段还缺标准 ID；会员类型筛选仍有动态生成和页面局部口径风险。 |
| 5 | 订场用户 | `public/assets/scripts/pages/courts.js`、`server/page-data/court-account-read-model.js` | 部分统一 | 账户类型、订场阶段、最近订场时间等缺标准 ID；导入仍兼容“账号、姓名”；详情会展示关联线索旧字段。 |
| 6 | 约球活动 | `public/assets/scripts/pages/matches.js`、`server/match-routes.js`、`wechat-miniprogram/` | 未完整纳入口径矩阵 | 状态已使用 `matchStatuses` 字典，但活动时间、场地、费用、报名人、结算字段缺标准 ID。 |
| 7 | 排课管理 | `public/assets/scripts/pages/schedule.js` | 未统一完成 | 表单和详情仍展示“姓名”；排课状态、课前教案、课后反馈、结算方式、场地费等选项仍在页面内定义或未登记成统一字典。 |
| 8 | 排课日历 | `public/assets/scripts/pages/coachops.js` | 未完整强约束 | 和排课共用事实数据，但日历卡片、日 / 周摘要、课程卡片字段未逐项绑定标准 ID。 |
| 9 | 教练课时统计 | `public/assets/scripts/pages/coachops.js` | 指标入口不唯一 | `coachTrialConversionText()` 在前端按排课和购买本地计算体验课转化率，和经营分析、教练工作台不是同一入口。 |
| 10 | 课包产品 | `public/assets/scripts/pages/packages.js` | 部分字典化 | 状态、时段部分来自统一字典；上课人数、工作日 / 周末、可用时段、适用教练、适用校区仍是页面配置或动态选项。 |
| 11 | 会员方案 | `public/assets/scripts/pages/courts.js` | 部分字典化 | 方案状态使用统一字典；折扣、赠送权益、会员档位、有效期等缺标准 ID。 |
| 12 | 财务总览 | `public/assets/scripts/pages/coachops.js`、`server/page-data/finance-page.js` | 相对收口，但需防回退 | 页面主要读财务统一层；但财务筛选仍从当前返回行动态生成，需要明确是动态维度还是统一字典。 |
| 13 | 收款流水 | `public/assets/scripts/pages/coachops.js` | 相对收口 | 导出和表格仍需纳入字段白名单；支付方式 / 业务类型要和统一字典、财务字段定义完全对齐。 |
| 14 | 入账流水 | `public/assets/scripts/pages/coachops.js` | 相对收口 | 需要和排课、课包、会员权益流水做同样本一致性测试，防止财务页正确但业务页不同步。 |
| 15 | 经营总览 | `public/assets/scripts/pages/operations.js`、`server/metrics/operations-metrics.js` | 后端统一较好 | 必须和线索池、学员页、教练端做跨页面一致性；否则只是经营分析内部统一。 |
| 16 | 场地运转 | `public/assets/scripts/pages/operations.js`、`server/metrics/operations-metrics.js` | 部分统一 | 场地利用率、黄金时段等在后端算；页面热力图带 `data-utilization / data-load / data-used-minutes` 调试型属性，需明确允许范围。 |
| 17 | 转化与留存 | `public/assets/scripts/pages/operations.js`、`server/metrics/operations-metrics.js` | 后端统一较好 | 已走经营分析模型，但线索池顶部没有强制读取同一个结果。 |
| 18 | 教练人效 | `public/assets/scripts/pages/operations.js`、`server/metrics/operations-metrics.js` | 未和教练端统一 | 经营分析、教练课时统计、教练工作台存在三处体验课转化率口径入口。 |
| 19 | 教练管理 | `public/assets/scripts/pages/coaches.js` | 未完整字典化 | 教练状态在页面内写死 `active / inactive`；教练基础资料字段缺标准 ID。 |
| 20 | 校区管理 | `public/assets/scripts/pages/campusmgr.js` | 未完整字典化 | 校区基础字段、场地配置字段未完整进入统一口径总表。 |
| 21 | 账号管理 | `public/assets/scripts/pages/admin-users.js` | 未完整字典化 | 角色、数据范围在页面内写死；权限字段缺标准 ID。 |

## 3. 侧栏外但必须覆盖的页面 / 面板

这些页面不全是侧栏直接入口，但代码里存在，且用户能通过按钮、路由、面包屑或历史入口进入；全仓库统一不能漏掉。

| 页面 / 面板 | 主要代码位置 | 是否已纳入统一 | 发现的问题 |
|---|---|---|---|
| 课程产品，隐藏入口 | `public/assets/scripts/pages/products.js` | 未纳入 21 页字段矩阵主体 | 类型来自 `PRODUCT_TYPES`，但课程名称、人数、定价、课时、备注等仍缺商品级标准 ID。 |
| 购买记录 | `public/assets/scripts/pages/purchases.js` | 未纳入 21 侧栏清单，但有页面区块和测试 | 购买详情展示“录入时间”；“学员”作为业务角色可保留，但主字段仍需和客户字段区分。 |
| 课包余额，隐藏入口 | `public/assets/scripts/pages/entitlements.js` | 未纳入 21 侧栏清单 | 剩余课时、有效期、时段、状态和正式学员 / 排课页必须跨页面一致；目前仍是独立页面渲染。 |
| 会员购买记录 | `public/assets/scripts/pages/courts.js` | 未纳入 21 侧栏清单 | 使用 `formatMembershipLedgerTime()` 格式化时间，但订单字段、权益摘要、覆盖范围需进字段白名单。 |
| 会员权益流水 | `public/assets/scripts/pages/courts.js` | 未纳入 21 侧栏清单 | 表格直接展示 `membershipOrderRef`，属于内部 ID 暴露风险，需确认是否运营必要。 |
| 价格方案 | `public/assets/scripts/pages/prices.js` | 未纳入原 21 页审计表 | 虽然价格类型、商品类型、渠道、状态多数字典化，但场地类型、日期类型、默认马坡价格模板仍在页面内写死。 |
| 教练结算面板 | `public/assets/scripts/pages/coachops.js` | 财务隐藏面板 | 不在侧栏 3 个财务入口中，但 `financePanel` 支持 `settlement`；教练结算字段需单独入矩阵。 |
| 权益账户兼容页 | `public/index.html`、`public/assets/scripts/pages/entitlements.js` | 隐藏保留 | 仍可能被历史入口访问，不能因为隐藏就跳过字段 / 指标统一。 |

## 4. 教练端入口审计

| 入口 | 主要代码位置 | 现状 | 发现的问题 |
|---|---|---|---|
| 工作台 | `public/assets/scripts/pages/coach-portal.js`、`api/index.js` | 未统一 | 工作台文案写“后续已购买任意产品的学员占比”；`api/index.js` 的 `buildWorkbenchStats()` 自己算体验课转化率。 |
| 课后评价 | `public/assets/scripts/pages/schedule.js`、`public/assets/scripts/pages/coach-portal.js` | 部分统一 | 体验课内部记录里的学员水平、转化意愿、推荐产品仍是页面内 select 选项。 |
| 我的学员 | `public/assets/scripts/pages/coach-portal.js`、`public/assets/scripts/pages/students.js` | 未完整交叉校验 | 必须和正式学员 / 普通学员的客户生命周期口径一致，现有测试未证明。 |
| 我的班次 | `public/assets/scripts/pages/coach-portal.js`、`public/assets/scripts/pages/schedule.js` | 未完整交叉校验 | “班次 / 学员”属于业务角色可保留，但必须和排课、课包、学员详情同源。 |

## 5. 已确认的不一致清单

### 5.1 字段名称不一致

1. 线索页仍有“意向等级”，标准字段应为“意向等级”。
   证据：`public/assets/scripts/pages/leads.js` 第 862、869、1059、1129 行。

2. 排课页仍有“姓名”。
   证据：`public/assets/scripts/pages/schedule.js` 第 539、1895 行。

3. 学员详情仍展示“需求产品”。
   证据：`public/assets/scripts/pages/students.js` 第 1009-1011 行。

4. 订场导入仍兼容“账号、姓名”等旧字段，且默认校区代码可落到 `shunyi_mapo`。
   证据：`public/assets/scripts/pages/courts.js` 第 2142-2147、2157、2265 行。

5. 后端线索导入仍要求旧列“需求产品、意向等级”。
   证据：`api/index.js` 第 5835-5837 行。

6. 测试本身还在保护旧字段“意向等级”。
   证据：`tests/leads-view.test.js` 第 130、152 行。

7. 排课测试本身还在保护“姓名”。
   证据：`tests/schedule-page-view.test.js` 第 96、133、210、261 行。

### 5.2 同一个指标仍有多套算法

1. 线索池顶部指标在 `leads.js` 本地算。
   证据：`public/assets/scripts/pages/leads.js` 第 738-763 行。

2. 普通 / 正式学员顶部统计在 `students.js` 本地按当前列表和前端数据汇总。
   证据：`public/assets/scripts/pages/students.js` 第 470-515 行。

3. 教练课时统计体验课转化率在 `coachops.js` 本地算。
   证据：`public/assets/scripts/pages/coachops.js` 第 451-470、652 行。

4. 教练工作台体验课转化率在 `api/index.js` 的 `buildWorkbenchStats()` 算。
   证据：`api/index.js` 第 827-885 行。

5. 经营分析教练人效在 `server/metrics/operations-metrics.js` 算。
   风险：教练课时统计、教练人效、教练工作台不是同一 read model 的纯展示。

6. 教练工作台文案说明“后续已购买任意产品”，和统一口径“体验后正式课包成交”等标准口径冲突。
   证据：`public/assets/scripts/pages/coach-portal.js` 第 21-22、118-122 行。

### 5.3 下拉筛选 / 选项未全部统一字典

已部分统一：

1. 线索来源、需求产品、意向等级、水平等来自 `business-taxonomy.js`。
2. 价格类型、价格商品类型、渠道、业务类型、价格状态来自 `business-taxonomy.js`。
3. 约球状态来自 `matchStatuses`。
4. 课包状态、时段来自统一字典。
5. 排课支付方式、取消原因等部分来自统一字典。

仍未统一或未登记：

1. 账号角色、数据范围：`public/assets/scripts/pages/admin-users.js` 第 265-267 行。
2. 教练状态：`public/assets/scripts/pages/coaches.js` 第 21 行。
3. 排课课前教案、课后反馈、状态筛选：`public/assets/scripts/pages/schedule.js` 第 8-12 行。
4. 排课结算方式、场地费：`public/assets/scripts/pages/schedule.js` 第 527、533 行。
5. 课后反馈里的学员水平、转化意愿、推荐产品：`public/assets/scripts/pages/schedule.js` 第 1675 行。
6. 订场流水内部占用原因、价格来源：`public/assets/scripts/pages/courts.js` 第 1927 行附近。
7. 价格页场地类型、日期类型：`public/assets/scripts/pages/prices.js` 第 132-134 行。
8. 课包工作日 / 周末、班型人数：`public/assets/scripts/pages/packages.js` 第 526-527、691-693 行。

### 5.4 原始值 / 内部痕迹展示风险

1. 购买详情展示“录入时间”。
   证据：`public/assets/scripts/pages/purchases.js` 第 461-464 行。

2. 订场导入默认校区代码为 `shunyi_mapo`，后续如果展示未经过 `cn()` 可能暴露内部代码。
   证据：`public/assets/scripts/pages/courts.js` 第 2145-2147 行。

3. 会员权益流水直接展示 `membershipOrderRef`，属于内部订单 ID 暴露风险。
   证据：`public/assets/scripts/pages/courts.js` 第 645 行。

4. 经营分析热力图 HTML 带 `data-utilization / data-load / data-used-minutes`。
   证据：`public/assets/scripts/pages/operations.js` 第 569 行。
   说明：这不是肉眼文案，但属于前端调试型属性，需要明确允许范围。

5. 全仓库大量 `createdAt / updatedAt / toISOString()` 是内部写入或测试样本，不能简单全禁。
   后续 CI 必须区分“内部存储允许”和“页面直接展示禁止”。

## 6. 测试覆盖缺口

当前已有：

1. `tests/cross-page-metric-consistency.test.js`
2. `tests/data-standards-governance.test.js`
3. `tests/data-standard-source-guard.test.js`
4. `tests/business-dictionary-global.test.js`
5. `tests/lead-field-standards.test.js`
6. 各页面 view / rules 测试

但缺口仍然存在：

1. 没有覆盖“21 个侧栏入口 + 侧栏外页面 + 教练端 + 小程序”的完整 CI 覆盖表。
2. 没有禁止 `leads.js` 本地计算线索池核心漏斗指标。
3. 没有禁止 `students.js` 把当前列表统计包装成全局指标。
4. 没有禁止 `coachops.js` / `api/index.js` 各自计算体验课转化率。
5. 没有检查所有下拉必须来自统一字典或登记为动态业务字典。
6. 没有检查列表、详情、弹窗、导出字段是否全部统一。
7. 没有检查“意向等级、需求产品、账号、姓名”等旧字段全仓库禁止；部分测试反而保护旧文案。
8. 没有检查“录入时间、内部 ID、流水 ID、ISO 时间、校区代码、调试属性”不得直接展示。
9. 没有把教练端工作台、课后评价、我的学员、我的班次纳入经营分析同样本一致性。
10. 微信小程序本次只做静态搜索，未做页面级字段 / 指标一致性测试。

## 7. 后端 / 导入 / 脚本审计结论

1. `server/read-models/customer-lifecycle.js`、`server/read-models/platform-metrics.js` 已经是统一方向，但前端页面没有被强制只能读它们。
2. `server/metrics/operations-metrics.js` 是经营分析权威入口之一，但线索池、学员页、教练端没有全部收敛到同一读模型。
3. `api/index.js` 仍包含教练工作台统计和旧导入字段，是后续高冲突文件。
4. `scripts/` 中大量修数、导入、回滚脚本使用内部字段和 ISO 时间属于正常存储 / 审计用途；后续门禁不能全仓库粗暴禁止，只能禁止“页面可见展示”和“核心指标重复计算”。
5. `wechat-miniprogram/` 未发现和“意向等级 / 需求产品 / 姓名”同类明显旧词，但仍未纳入字段矩阵和跨端一致性测试。

## 8. 和其他线程的冲突风险

如果下一步做 CI 或代码收口，高概率碰这些文件：

1. `public/assets/scripts/pages/leads.js`
2. `public/assets/scripts/pages/students.js`
3. `public/assets/scripts/pages/schedule.js`
4. `public/assets/scripts/pages/courts.js`
5. `public/assets/scripts/pages/coach-portal.js`
6. `public/assets/scripts/pages/coachops.js`
7. `public/assets/scripts/pages/prices.js`
8. `public/assets/scripts/pages/packages.js`
9. `public/assets/scripts/pages/purchases.js`
10. `public/assets/scripts/core/business-taxonomy.js`
11. `api/index.js`
12. `server/metrics/operations-metrics.js`
13. `server/read-models/platform-metrics.js`
14. `server/read-models/customer-lifecycle.js`
15. `tests/cross-page-metric-consistency.test.js`
16. `tests/data-standard-source-guard.test.js`
17. `tests/business-dictionary-global.test.js`
18. `tests/leads-view.test.js`
19. `tests/schedule-page-view.test.js`
20. `tests/coach-portal-view.test.js`

当前本报告只新增 / 修改文档和 `.gitignore` 白名单，不改业务代码。

## 9. 修复优先级

### P0：先修会继续误导用户的口径问题

1. 禁止“意向等级”，统一为“意向等级”，同时修掉保护旧文案的测试。
2. 排课主字段从“姓名”收口为“姓名”，但保留“学员”作为教学业务角色时必须写明规则。
3. 禁止线索池顶部在 `leads.js` 本地算核心漏斗指标。
4. 禁止教练工作台和教练课时统计各算一套体验课转化率。
5. 普通 / 正式学员顶部统计必须明确是“当前列表统计”还是“全局标准指标”，不能混叫。
6. 购买记录、会员权益流水、价格页、隐藏权益页纳入字段矩阵和门禁。

### P1：补强 CI 门禁

1. 建“21 个侧栏入口 + 侧栏外页面 + 教练端”的字段 / 筛选 / 卡片 / 详情 / 导出覆盖测试。
2. 建同样本跨页面一致性：线索池、普通学员、正式学员、转化与留存、经营总览、教练人效、教练工作台。
3. 建旧字段禁止测试：`意向等级 / 需求产品 / 账号 / 姓名 / 姓名 / 姓名`。
4. 建原始值展示禁止测试：页面可见文案不得直接展示 ISO 时间、内部 ID、流水 ID、校区代码、调试字段。
5. 建核心公式唯一性测试：核心指标只能在统一 read model / metrics 模型计算。
6. 建下拉字典来源测试：页面静态下拉必须来自 `business-taxonomy.js` 或字段矩阵登记。

### P2：按清单逐项修代码

1. 统一字段展示名。
2. 统一下拉来源。
3. 统一指标 read model。
4. 统一导入兼容字段映射。
5. 补齐待补标准 ID。
6. 每修一项就加回归测试，防止门禁再次只覆盖局部。

## 10. 本次没有做的事

1. 没有修业务代码。
2. 没有跑全量测试。
3. 没有启动本地服务。
4. 没有访问生产数据。
5. 没有浏览器逐页验收。
6. 没有宣称“全局统一已完成”。
