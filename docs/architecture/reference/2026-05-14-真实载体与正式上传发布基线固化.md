# 2026-05-14 真实载体与正式上传发布基线固化

> 目标：把 FlowTennis 当前真实载体、正式上传目录、正式发布基线、工作区边界一次性写死，后续不再靠记忆判断，不再从脏目录误上传。

---

## 1. 当前真实上传 / 发布目录总表

| 类型 | 正式目录 | 用途 | 当前结论 |
| --- | --- | --- | --- |
| Web 正式发布工程目录 | `/Users/shaobaolu/Desktop/FlowTennis/flowtennis-mgmt-main` | Vercel 发布工程根目录，实际发布内容包含 `public/` 和 `api/` | 真实 Web 发布工程，只认这一套仓库 |
| Web 前端真实载体目录 | `/Users/shaobaolu/Desktop/FlowTennis/flowtennis-mgmt-main/public` | 管理端 Web / 教练端网页 / 教练端 PWA 共用前端壳 | 真实前端载体，只认这一套目录 |
| 教练端小程序正式上传工程目录 | `/Users/shaobaolu/Desktop/FlowTennis/flowtennis-mgmt-main/wechat-miniprogram` | 微信开发者工具打开的工程目录 | 真实教练端小程序上传目录 |
| 教练端小程序实际打包目录 | `/Users/shaobaolu/Desktop/FlowTennis/flowtennis-mgmt-main/wechat-miniprogram/miniprogram` | 小程序代码包根目录 | 真实教练端小程序载体 |
| 约球小程序正式上传工程目录 | `/Users/shaobaolu/Desktop/FlowTennis/flowtennis-mini-match` | 微信开发者工具打开的工程目录 | 真实约球小程序上传目录 |
| 约球小程序实际打包目录 | `/Users/shaobaolu/Desktop/FlowTennis/flowtennis-mini-match/miniprogram` | 小程序代码包根目录 | 真实约球小程序载体 |

---

## 2. 目录分类：开发目录 / 上传副本 / 历史遗留

### 2.1 开发目录

1. `/Users/shaobaolu/Desktop/FlowTennis/flowtennis-mini-match`
2. `flowtennis-mgmt-main` 家族后续新建的任务 worktree：`/Users/shaobaolu/Desktop/FlowTennis/flowtennis-mgmt-main/.worktrees/<任务名>`

说明：

- `flowtennis-mini-match` 仍是约球小程序正式开发目录。
- `flowtennis-mgmt-main` 家族后续不再使用旧主目录承载日常开发，只允许使用从正式基线新切出的 worktree。
- 开发目录默认不能直接作为“看到就上传”的正式上传目录。

### 2.2 当前已确认的上传副本

1. `/Users/shaobaolu/Desktop/FlowTennis/flowtennis-mgmt-main/.worktrees/thread114-staging-merge`

说明：

- 这个工作区当前对齐 `origin/thread26-staging-latest` 的正式主线收官锚点提交，可作为 `flowtennis-mgmt-main` 家族的干净上传核对副本。
- 它现在正式定性为 `flowtennis-mgmt-main` 家族唯一正式干净母本。
- 它的用途固定为：
  - 正式验收目录
  - 正式上传核对目录
  - 干净工作区体系母本
- 它不是日常开发堆改动的目录，不允许在这里堆长期开发残留。

### 2.3 当前已确认的约球小程序上传副本

1. `/Users/shaobaolu/Desktop/FlowTennis/flowtennis-mini-match-release`

说明：

- 这是约球小程序唯一正式上传副本。
- `flowtennis-mini-match` 是正式开发目录和真实业务基线目录，但不是正式上传目录。
- 每次上传前，必须先把目标版本固定为明确提交，再用 `scripts/sync-release-worktree.sh` 把同一提交同步到 `flowtennis-mini-match-release`。
- `flowtennis-mini-match-release` 只要存在未提交改动，就禁止上传。

### 2.4 历史遗留 / 禁止继续作为正式载体或正式上传目录

1. `/Users/shaobaolu/Desktop/FlowTennis/.worktrees/`
2. `/Users/shaobaolu/Desktop/FlowTennis/flowtennis-mgmt-main/.worktrees/` 下除 `thread114-staging-merge` 外的所有目录
3. `/Users/shaobaolu/Desktop/FlowTennis/flowtennis-mgmt-main/_local/legacy-2026-04-10/`
4. `/Users/shaobaolu/Desktop/FlowTennis/flowtennis-mgmt-main/_local/requirements/`
5. `/Users/shaobaolu/Desktop/FlowTennis/flowtennis-mgmt-main/public/index.backup-before-split.html`
6. 当前仍停在 `codex/match-real-launch` 的 `flowtennis-mgmt-main` 主工作区

说明：

- 这些目录只允许历史取证、差异对比、资料留档。
- 禁止继续当成正式上传目录。
- 禁止继续当成正式发布基线。
- 其中 `/Users/shaobaolu/Desktop/FlowTennis/flowtennis-mgmt-main` 已正式退役为旧线只读取证区：
  - 禁止开发
  - 禁止联调
  - 禁止验收
  - 禁止上传
  - 只允许历史取证和差异比对

---

## 3. 正式发布基线清单

### 3.1 `flowtennis-mgmt-main` 家族正式发布基线

适用对象：

1. 管理端 Web
2. 教练端网页
3. 教练端 PWA
4. 教练端小程序

正式发布基线固定为：

1. 基线分支锚点：`origin/thread26-staging-latest`
2. 当前确认提交：`8212c860b7eab3fa5ddfd8ce8ef6193bd1e6e5f0`
3. 上传前核对工作区：`/Users/shaobaolu/Desktop/FlowTennis/flowtennis-mgmt-main/.worktrees/thread114-staging-merge`

硬规则：

1. 新 `main` 正式恢复前，这一组端的正式发布基线只认 `origin/thread26-staging-latest`。
2. `codex/match-real-launch`、旧 `main`、其他历史 worktree 都不是这一组端的正式发布基线。
3. 真机、体验版、Web 正式站、上传工作区，必须都能回指到同一个基线提交。

### 3.2 `flowtennis-mini-match` 正式发布基线

适用对象：

1. 约球小程序

当前正式发布基线固定为：

1. 真实工程：`/Users/shaobaolu/Desktop/FlowTennis/flowtennis-mini-match`
2. 当前可追溯基线提交：`57a236955a930fa57231916f6a790658aa250492`
3. 当前真实 AppID：`wxec5ca996f273502b`
4. 当前默认首页：`pages/matches/index`
5. 唯一正式上传副本：`/Users/shaobaolu/Desktop/FlowTennis/flowtennis-mini-match-release`

硬规则：

1. 约球小程序正式发布基线只认 `flowtennis-mini-match`。
2. `flowtennis-mgmt-main/wechat-miniprogram` 不是约球小程序发布基线。
3. `flowtennis-mini-match-release` 是约球小程序唯一正式上传副本，上传只允许从这里进行。
4. `flowtennis-mini-match-release` 必须保持干净；只要存在未提交改动，就禁止上传。
5. 下次上传前，必须先把目标版本固定成一个明确提交，再同步到 `flowtennis-mini-match-release` 后上传；禁止直接拿当前脏工作区上传。

---

## 4. 以后上传和发版的唯一标准动作

### 4.1 Web / 教练端网页 / 教练端 PWA / 教练端小程序

1. 先确认目标版本只能来自 `origin/thread26-staging-latest`，或新 `main` 恢复后来自恢复后的正式 `main`。
2. 先看上传工作区是否干净；只允许用和正式基线提交完全一致的干净工作区做上传核对。
3. 如果开发目录有未提交改动，禁止直接上传，必须改到干净工作区复刻同一提交再上传。
4. Web 发布只认 `flowtennis-mgmt-main` 工程；前端真实载体只认 `public/`，后端真实发布入口只认 `api/`。
5. 教练端小程序上传只认 `flowtennis-mgmt-main/wechat-miniprogram` 工程；默认入口只认 `pages/index/index`。
6. 上传完成后，体验版 / 真机 / 正式站都必须能回指到同一提交号。
7. 当前仍停在 `codex/match-real-launch` 的 `flowtennis-mgmt-main` 工作区只允许只读比对，禁止继续开发、验收、上传、发版。

### 4.1.1 老板切换到正式 worktree 流程的最短 3 步

1. 停用旧主目录 `/Users/shaobaolu/Desktop/FlowTennis/flowtennis-mgmt-main`，不再在这里开发、验收、上传。
2. 验收和上传统一改到 `/Users/shaobaolu/Desktop/FlowTennis/flowtennis-mgmt-main/.worktrees/thread114-staging-merge`。
3. 后续新需求一律从 `origin/thread26-staging-latest`，或新 `main` 恢复后的正式 `main`，新建 worktree 到 `flowtennis-mgmt-main/.worktrees/<任务名>`。

### 4.1.2 以后新线程创建 worktree 的唯一标准步骤

1. 先确认起点分支只允许是 `origin/thread26-staging-latest`，或新 `main` 恢复后的正式 `main`。
2. 新工作区目录只允许建在 `/Users/shaobaolu/Desktop/FlowTennis/flowtennis-mgmt-main/.worktrees/<任务名>`。
3. 新线程开发只在该新 worktree 内进行。
4. 验收和上传仍只认 `/Users/shaobaolu/Desktop/FlowTennis/flowtennis-mgmt-main/.worktrees/thread114-staging-merge`。

### 4.2 约球小程序

1. 先确认目标版本只来自 `flowtennis-mini-match`。
2. 先确认目标提交号，禁止靠“当前目录里看起来像最新”来判断。
3. 当前工作区如果有本地残留改动，禁止直接上传。
4. 先运行 `flowtennis-mini-match/scripts/sync-release-worktree.sh <提交号>`，把目标提交同步到 `flowtennis-mini-match-release`。
5. 微信开发者工具正式上传只允许打开 `flowtennis-mini-match-release`，不允许直接打开 `flowtennis-mini-match` 上传。
6. `flowtennis-mgmt-main/wechat-miniprogram` 不允许代替约球上传。
7. 默认首页只认 `pages/matches/index`，AppID 只认 `wxec5ca996f273502b`。
8. 上传完成后，体验版 / 真机 / 本地核对目录必须是同一提交快照。

---

## 5. 一句话硬结论

1. Web 与教练端小程序家族的正式发布基线，只认 `origin/thread26-staging-latest` 上的 `8212c860b7eab3fa5ddfd8ce8ef6193bd1e6e5f0`。
2. 约球小程序的真实载体与正式发布基线，只认 `flowtennis-mini-match`；唯一正式上传副本只认 `flowtennis-mini-match-release`。
3. 当前仍停在 `codex/match-real-launch` 的 `flowtennis-mgmt-main` 工作区只允许只读取证，禁止继续开发或上传。
4. 主开发目录可以脏，但正式上传目录必须是同一提交的干净快照。
5. 以后凡是“上传错目录、工作区脏、版本混线”，都按违反本文档处理。
6. `flowtennis-mgmt-main` 家族的旧主目录脏问题，已经通过“旧线退役 + 正式母本固定 + 新线程只走 worktree”完成流程层收口。
